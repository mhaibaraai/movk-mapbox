---
title: MaplibreTemperature
description: Render temperature sample points as a heatmap layer with weight range, radius, and color stops.
category: weather
seo:
  title: MaplibreTemperature Component
  description: Render temperature sample points as a heatmap layer with weight range, radius and color stops.
---

## Introduction

`MaplibreTemperature` renders GeoJSON point features as a native MapLibre heatmap based on a temperature property. `weightProperty` specifies the weight field, and `weightRange` linearly maps its values to heatmap weights. `radius`, `intensity`, `opacity`, and `colorStops` control the visual output.

## Usage

Visualize the heatmap distribution of city temperature samples — adjust `radius` to see how the spread changes:

::component-example
---
name: TemperatureExample
prettier: true
options:
  - name: radius
    label: radius
    default: 40
    items:
      - 20
      - 40
      - 60
---
::

## API

### Props

:component-props{slug="MaplibreTemperature"}

## Changelog

:commit-changelog{prefix="components/environment" name="Temperature"}
