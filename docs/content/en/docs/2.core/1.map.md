---
title: MaplibreMap
description: The root component that creates a MapLibre GL instance on the client and distributes it via MaplibreContext, with v-model camera two-way binding and cross-route persistence.
category: basics
seo:
  title: MaplibreMap Component
  description: MaplibreMap is the root component that creates a maplibre-gl instance on the client and provides MaplibreContext to descendants, with v-model camera binding, persistent reuse and SSR-safe rendering.
---

## Introduction

`MaplibreMap` is the root of everything: it creates the `maplibre-gl` instance on the client in `onMounted`, distributes a [MaplibreContext](/docs/getting-started/concepts) via `provide`, and child components access it through `useMap()`. The container is sized at `100%` width and height — make sure the parent has an explicit height (examples use `h-115` throughout).

::note
The component is SSR-safe and does not require a `<ClientOnly>` wrapper. When `options.style` is omitted, a blank style is used (pair it with [MaplibreTiandituLayer](/docs/extensions/tianditu) to overlay raster basemaps only).
::

## Usage

`center` / `zoom` / `bearing` / `pitch` all support `v-model`: the component compares the bound value against the current map state and only pushes an update when they differ, breaking the "model → map → event → model" feedback loop. Dragging or zooming the map keeps the bound values in sync.

::component-example
---
name: MapBasicExample
prettier: true
---
::

## Examples

### Camera Transitions

Use `flyTo` from `useMaplibreCamera` to smoothly animate between multiple preset camera positions:

::component-example
---
name: MapCameraExample
prettier: true
collapse: true
---
::

## API

### Props

:component-props{slug="MaplibreMap"}

### Emits

`update:center` / `update:zoom` / `update:bearing` / `update:pitch` are the camera `v-model` sync events. All other events are forwarded maplibre-gl map events.

:component-emits{slug="MaplibreMap"}

### Slots

:component-slots{slug="MaplibreMap"}

## Changelog

:commit-changelog{prefix="components"}
