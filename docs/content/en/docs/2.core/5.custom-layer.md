---
title: MaplibreCustomLayer
description: Hosts a CustomLayerInterface and wires a custom WebGL layer into the declarative lifecycle and style-reload rebuilding.
category: basics
seo:
  title: MaplibreCustomLayer Component
  description: MaplibreCustomLayer hosts a maplibre-gl CustomLayerInterface, wiring custom WebGL rendering into the declarative mount, unmount and style-reload lifecycle.
---

## Introduction

`MaplibreCustomLayer` is the escape hatch for `CustomLayerInterface`: it wires your custom WebGL layer (with its own `onAdd` / `render` callbacks) into the component lifecycle — calling `addLayer` when the map is ready, removing it on unmount, and rebuilding via `onReady` after a basemap switch. Suitable for deep custom rendering with Three.js, deck.gl, or raw WebGL.

## Usage

The example below uses raw WebGL to draw a semi-transparent triangle in map coordinates:

::component-example
---
name: CustomLayerExample
prettier: true
collapse: true
---
::

## API

### Props

:component-props{slug="MaplibreCustomLayer"}

### Slots

:component-slots{slug="MaplibreCustomLayer"}

## Changelog

:commit-changelog{prefix="components"}
