---
title: MapboxMarker
description: A marker that renders custom DOM through its default slot, with v-model:lnglat two-way binding and drag-to-update support.
category: overlay
seo:
  title: MapboxMarker Component
  description: MapboxMarker renders a mapbox-gl marker with custom DOM via the default slot, two-way v-model:lnglat binding and drag-to-update support.
---

## Introduction

`MapboxMarker` places a marker on the map: the default slot provides custom DOM content (omitting it falls back to the mapbox default teardrop icon), and the position is two-way bound via `v-model:lnglat`. When `options.draggable` is enabled, the new coordinates are written back to the bound value after each drag ends.

## Usage

Without a slot, the default marker is used and `v-model:lnglat` controls the position:

::component-example
---
name: MarkerBasicExample
prettier: true
---
::

## Examples

### Custom Content and Dragging

The default slot renders custom DOM. With `draggable` enabled, coordinates are written back in real time as the marker is dragged:

::component-example
---
name: MarkerDraggableExample
prettier: true
collapse: true
---
::

## API

### Props

:component-props{slug="MapboxMarker"}

### Emits

:component-emits{slug="MapboxMarker"}

### Slots

:component-slots{slug="MapboxMarker"}

### Expose

Access the component instance via [`useTemplateRef`](https://vuejs.org/api/composition-api-helpers.html#usetemplateref).

| Name | Type |
| ---- | ---- |
| `marker`{lang="ts-type"} | `() => Marker \| undefined`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>Returns the underlying mapbox-gl Marker instance</p></div> |

## Changelog

:commit-changelog{prefix="components"}
