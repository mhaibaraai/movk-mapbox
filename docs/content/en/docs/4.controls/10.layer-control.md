---
title: MaplibreLayerControl
description: Layer control that lists titled layer groups with visibility checkboxes and opacity sliders.
category: layer-management
seo:
  title: MaplibreLayerControl Component
  description: MaplibreLayerControl lists titled layer groups with visibility checkboxes and opacity sliders that write back to each group's v-model.
---

## Introduction

`MaplibreLayerControl` lists every [`MaplibreLayerGroup`](/docs/core/layer-group) with a `title` on the map, in declaration order. Checkboxes toggle visibility and sliders set opacity, and the results are written back to each group's `v-model:visible` and `v-model:opacity`; the control never edits the style itself. It therefore stays in sync with external buttons, sidebar panels ([`useLayerTree`](/docs/composables/use-layer-tree)) and the legend, and its state survives basemap switches.

::note
Layers that ship with the basemap style (labels, roads and so on) can appear in the list once a layer group adopts them through `styleLayers`; see [Control basemap layers](/docs/core/layer-group).
::

## Usage

::component-example
---
name: LayerControlExample
prettier: true
collapse: true
---
::

## API

### Props

:component-props{slug="MaplibreLayerControl"}

### Emits

:component-emits{slug="MaplibreLayerControl"}

### Slots

:component-slots{slug="MaplibreLayerControl"}

## Changelog

:commit-changelog{prefix="components/controls" name="LayerControl"}
