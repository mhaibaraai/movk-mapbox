---
title: MaplibreNavigationControl
description: Navigation control that provides zoom buttons and a compass. Use position to set the dock corner and options to pass through to maplibre-gl.
category: interactive
seo:
  title: MaplibreNavigationControl Component
  description: MaplibreNavigationControl adds maplibre-gl zoom buttons and a compass, with a position prop for docking and options forwarded to the underlying NavigationControl.
---

## Introduction

`MaplibreNavigationControl` mounts zoom buttons and a compass on the map. `position` sets the dock corner (omit to use the map's default), and `options` is forwarded directly to the maplibre-gl `NavigationControl` constructor (`showZoom` / `showCompass` / `visualizePitch`). The control is mounted when the map is ready and automatically removed when the component unmounts.

## Usage

Adjust `position` to change the dock corner:

::component-example
---
name: NavigationControlExample
prettier: true
options:
  - name: position
    label: position
    default: top-right
    items:
      - top-left
      - top-right
      - bottom-left
      - bottom-right
---
::

## Examples

### Visualize Pitch

Tilt the map and enable `visualizePitch` — the compass tilts along with the pitch angle to indicate the current view:

::component-example
---
name: NavigationControlPitchExample
prettier: true
collapse: true
---
::

## API

### Props

:component-props{slug="MaplibreNavigationControl"}

## Changelog

:commit-changelog{prefix="components/controls" name="NavigationControl"}
