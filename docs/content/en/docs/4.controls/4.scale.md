---
title: MaplibreScaleControl
description: Scale bar control that displays the current map scale and updates on zoom, supporting metric, imperial, and nautical units.
category: info
seo:
  title: MaplibreScaleControl Component
  description: MaplibreScaleControl shows the current map scale and updates with zoom, supporting metric, imperial and nautical units via options.
---

## Introduction

`MaplibreScaleControl` displays the current map scale and updates as you zoom. `options.unit` selects the unit (`metric` / `imperial` / `nautical`), and `options.maxWidth` limits the maximum pixel width of the scale bar.

## Usage

Switch `unit` to see the scale bar in different units:

::component-example
---
name: ScaleControlExample
prettier: true
options:
  - name: unit
    label: unit
    default: metric
    items:
      - metric
      - imperial
      - nautical
---
::

## API

### Props

:component-props{slug="MaplibreScaleControl"}

## Changelog

:commit-changelog{prefix="components/controls" name="ScaleControl"}
