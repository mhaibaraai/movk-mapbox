---
title: Introduction
description: Learn about Movk MapLibre's declarative design, context injection architecture, and dual Nuxt / Vue modes — from installation to composing maps, sources, layers, and controls.
seo:
  title: Introduction
  description: Movk MapLibre is a declarative MapLibre GL wrapper that provides MaplibreMap / MaplibreSource / MaplibreLayer components and composables, with native Nuxt 4 support and a Vite plugin for plain Vue + Vite projects.
---

## Introduction

Movk MapLibre is a **declarative [MapLibre GL JS v6](https://maplibre.org/maplibre-gl-js/docs/) wrapper library**. It replaces imperative `map.addSource` / `map.addLayer` / marker and control management with a composable set of Vue components and composables: use `MaplibreMap` to create a map, then declaratively compose map content with `MaplibreSource`, `MaplibreLayer`, `MaplibreMarker`, `MaplibrePopup`, and various controls — state is view. It also ships built-in 3D buildings, dynamic effects like radar / diffusion / glow, fog / terrain / weather environments, and localization capabilities including Tianditu, WMS / WMTS, and multi-CRS coordinate conversion.

It supports **two usage modes**:

- **Nuxt 4** — as a Nuxt module, with components and composables auto-imported out of the box.
- **Plain Vue + Vite** — via `@movk/maplibre/vite` + `@movk/maplibre/vue-plugin`, the same components and usage patterns work in non-Nuxt projects.

::callout{icon="i-lucide-rocket" color="primary"}
Designed for **progressive adoption**: you can use just the coordinate conversion utilities, or go all the way with layers, effects, drawing, and localized basemaps. Start with [Installation](/docs/getting-started/installation), then follow the [Nuxt](/docs/getting-started/nuxt) or [Vue / Vite](/docs/getting-started/vue) setup guide.
::

## Key Features

::card-group
  :::card{icon="i-lucide-blocks" title="Declarative Composition"}
  Compose maps, sources, layers, markers, popups, and controls as components — no more imperative add/remove boilerplate.
  :::

  :::card{icon="i-lucide-share-2" title="MaplibreContext Injection"}
  The root component provides context down the tree; child components access the instance, `isLoaded`, and `whenLoaded()` via `useMap()` — no id lookups needed.
  :::

  :::card{icon="i-lucide-move" title="Two-Way Camera Binding"}
  `v-model:center` / `zoom` / `bearing` / `pitch` sync the camera bidirectionally, with cross-route `persistent` support.
  :::

  :::card{icon="i-lucide-layers" title="Rich Layers & Effects"}
  Built-in layer types with clustering / buildings / raster / video, plus dynamic effects like radar, diffusion, glow, and migration, as well as sky / terrain / temperature environments.
  :::

  :::card{icon="i-lucide-map" title="Localization Extensions"}
  Tianditu basemaps, WMS / WMTS services, terra-draw integration, and multi-CRS coordinate conversion via gcoord.
  :::
::

## Capability Overview

::note{icon="i-lucide-layers"}
**Documentation Structure**

- **Core Components** — Map, Source, Layer, LayerGroup, CustomLayer, Marker, LottieMarker, Popup, Tooltip
- **Layer Components** — Built-in types (circle/fill/line/symbol/fill-extrusion/heatmap) and data-driven layers (cluster/building/image/video/raster)
- **Controls** — Navigation / Geolocate / Fullscreen / Scale / Attribution
- **Effect Components** — Radar, diffusion, glow, ripple, trail, migration, and building effects
- **Environment** — Sky, Terrain, Temperature
- **Extensions** — DrawControl, Tianditu, WMS/WMTS, and coordinate conversion
- **Composables & Utilities** — useMap, useMaplibreCamera, useMeasure, and coordinate / buffer / geometry tools
::
