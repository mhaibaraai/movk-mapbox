---
title: fill-extrusion
description: Extrude polygons into 3D volumes with MapboxLayer type="fill-extrusion", with data-driven height support.
category: builtin
seo:
  title: Fill Extrusion Layer
  description: Extrude polygons into 3D volumes with MapboxLayer type="fill-extrusion", driving height from feature properties and viewing with map pitch.
---

## Introduction

Set `type="fill-extrusion"` on `MapboxLayer` to extrude polygons into 3D volumes. `fill-extrusion-height` reads feature properties via `['get', ...]` to set per-feature heights. A non-zero map `pitch` is required to see the 3D effect. For full 3D city buildings, see [building](/docs/layers/building).

::note{to="/docs/core/layer"}
Fill-extrusion layers are powered by the generic MapboxLayer component. See its documentation for full Props and events reference.
::

## Usage

Two volumes extruded from their `height` property, with the map pitched at 55°:

::component-example
---
name: LayerFillExtrusionExample
prettier: true
---
::

## API

### Props

:component-props{slug="MapboxLayer"}

## Changelog

:commit-changelog{prefix="components" name="Layer"}
