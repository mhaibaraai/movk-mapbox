---
title: symbol
description: Render text and icon labels with MaplibreLayer type="symbol", reading feature properties via layout expressions.
category: builtin
seo:
  title: Symbol Layer
  description: Render text and icon labels with MaplibreLayer type="symbol", reading feature properties via text-field and layout expressions.
---

## Introduction

Set `type="symbol"` on `MaplibreLayer` to render text and icon labels. `text-field` reads feature properties via `['get', ...]`; layout properties like `text-size` and `text-offset` control placement. Icons must be registered first via [useMaplibreImage](/docs/composables/use-maplibre-image).

::note{to="/docs/core/layer"}
Symbol layers are powered by the generic MaplibreLayer component. See its documentation for full Props and events reference.
::

## Usage

Render text labels from feature properties, with a halo for readability on both light and dark basemaps:

::component-example
---
name: LayerSymbolExample
prettier: true
---
::

## API

### Props

:component-props{slug="MaplibreLayer"}

## Changelog

:commit-changelog{prefix="components" name="Layer"}
