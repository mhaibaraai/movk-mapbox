---
title: MapboxBuildingLayer
description: 3D building layer that extrudes the official composite/building vector source with configurable color, opacity, and minimum zoom.
category: data-driven
seo:
  title: MapboxBuildingLayer Component
  description: MapboxBuildingLayer extrudes the official composite/building vector source into 3D buildings with configurable color, opacity and minzoom.
---

## Introduction

`MapboxBuildingLayer` extrudes the `composite` / `building` vector source from Mapbox's official styles into 3D buildings. Height expressions are built in; `color`, `opacity`, and `minzoom` are available as convenience props.

::note{color="warning"}
This component depends on the `composite/building` source from Mapbox's official styles and will not work with empty styles such as Tianditu. A non-zero map `pitch` and sufficient `zoom` (default minzoom 15) are required to see buildings.
::

## Usage

Render 3D city buildings in a pitched view:

::component-example
---
name: BuildingLayerExample
prettier: true
---
::

## API

### Props

:component-props{slug="MapboxBuildingLayer"}

## Changelog

:commit-changelog{prefix="components/layers" name="BuildingLayer"}
