---
title: MaplibreDrawControl
description: Declarative drawing control built on terra-draw, with v-model features and mode, a built-in mode toolbar and imperative methods.
category: draw
seo:
  title: MaplibreDrawControl Component
  description: Declarative drawing control built on terra-draw, with v-model features and mode, a built-in mode toolbar and imperative methods.
links:
  - label: terra-draw
    icon: i-simple-icons-github
    to: https://github.com/JamesLMilner/terra-draw
    target: _blank
---

## Introduction

`MaplibreDrawControl` provides declarative drawing on top of [terra-draw](https://github.com/JamesLMilner/terra-draw): `v-model:features` is a controlled feature collection (assign to import, draw to write back) and `v-model:mode` tracks the current mode. It emits finish / delete / select / deselect / modechange events and exposes imperative methods via `defineExpose`. The default mode set is [movkDrawModes](/docs/extensions/draw-modes), `controls` configures the built-in toolbar, and features and mode are restored automatically after a basemap switch (`setStyle`). Child components can inject the draw context with [useMaplibreDraw](/docs/composables/use-maplibre-draw); once the parent map sets `map-id`, the composable can also drive drawing by id from outside the component tree.

::note
Install the optional dependencies `terra-draw` and `terra-draw-maplibre-gl-adapter`. Mode names follow terra-draw: `select` / `point` / `linestring` / `polygon` / `rectangle` / `circle` / `ellipse` / `sector`.
::

## Usage

Draw points, lines, polygons and regular shapes with the built-in toolbar; `v-model:features` writes back the feature count in real time:

::component-example
---
name: DrawControlExample
prettier: true
clientOnly: true
---
::

## Examples

### Imperative Actions

Grab the instance with [`useTemplateRef`](https://vuejs.org/api/composition-api-helpers.html#usetemplateref) and call `changeMode` / `deleteAll` (the built-in toolbar is disabled with `controls` set to `false`):

::component-example
---
name: DrawControlActionsExample
prettier: true
collapse: true
clientOnly: true
---
::

## API

### Props

:component-props{slug="MaplibreDrawControl"}

### Emits

:component-emits{slug="MaplibreDrawControl"}

### Slots

:component-slots{slug="MaplibreDrawControl"}

### Expose

Access the component instance via [`useTemplateRef`](https://vuejs.org/api/composition-api-helpers.html#usetemplateref).

| Name | Type |
| ---- | ---- |
| `draw`{lang="ts-type"} | `Readonly<Ref<TerraDraw \| undefined>>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>The underlying TerraDraw instance; undefined before mount</p></div> |
| `whenReady`{lang="ts-type"} | `() => Promise<TerraDraw>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>Resolves once the draw instance is ready</p></div> |
| `getAll`{lang="ts-type"} | `() => FeatureCollection \| undefined`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>Completed features, excluding in-progress features and guidance points</p></div> |
| `getMode`{lang="ts-type"} | `() => string \| undefined`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>The current draw mode</p></div> |
| `add`{lang="ts-type"} | `(geojson: Feature \| FeatureCollection \| Geometry) => Promise<FeatureId[]>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>Adds features and syncs the model; generates missing ids, infers a missing properties.mode from the geometry type, and returns the ids that were added</p></div> |
| `deleteAll`{lang="ts-type"} | `() => Promise<void>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>Removes all features and syncs the model</p></div> |
| `changeMode`{lang="ts-type"} | `(mode: string) => Promise<void>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>Switches the draw mode and syncs the model</p></div> |
| `setFeatureProperty`{lang="ts-type"} | `(featureId: FeatureId, property: string, value: unknown) => Promise<void>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>Sets a feature property (for example color to override the theme) and syncs the model</p></div> |

## Changelog

:commit-changelog{prefix="components/extensions" name="DrawControl"}
