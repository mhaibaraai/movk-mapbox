---
title: MapboxDrawControl
description: Declarative drawing control built on mapbox-gl-draw, with v-model features and mode plus imperative instance methods.
category: draw
seo:
  title: MapboxDrawControl Component
  description: Declarative drawing control built on mapbox-gl-draw, with v-model features and mode plus imperative instance methods.
links:
  - label: mapbox-gl-draw
    icon: i-simple-icons-mapbox
    to: https://github.com/mapbox/mapbox-gl-draw
    target: _blank
---

## Introduction

`MapboxDrawControl` provides declarative drawing on top of [mapbox-gl-draw](https://github.com/mapbox/mapbox-gl-draw): `v-model:features` controls the feature collection (assigning imports, drawing writes back), `v-model:mode` controls the current mode; it emits create / update / delete / selectionchange / modechange events; and exposes imperative methods via `defineExpose`. Child components can inject the draw context with [useMapboxDraw](/docs/composables/use-mapbox-draw); once the parent map sets a `map-id`, that composable can also drive drawing from outside the component tree.

## Usage

Draw points, lines, and polygons with the built-in toolbar — `v-model:features` reflects the live feature count:

::component-example
---
name: DrawControlExample
prettier: true
clientOnly: true
---
::

## Examples

### Imperative Actions

Access the instance via [`useTemplateRef`](https://vuejs.org/api/composition-api-helpers.html#usetemplateref) to call `changeMode` / `deleteAll` (default toolbar is disabled):

::component-example
---
name: DrawControlActionsExample
prettier: true
collapse: true
clientOnly: true
---
::

## API

### Props

:component-props{slug="MapboxDrawControl"}

### Emits

:component-emits{slug="MapboxDrawControl"}

### Slots

:component-slots{slug="MapboxDrawControl"}

### Expose

Access the component instance via [`useTemplateRef`](https://vuejs.org/api/composition-api-helpers.html#usetemplateref).

| Name | Type |
| ---- | ---- |
| `draw`{lang="ts-type"} | `Readonly<Ref<MapboxDraw \| undefined>>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>Reference to the underlying MapboxDraw instance; undefined before mount</p></div> |
| `whenReady`{lang="ts-type"} | `() => Promise<MapboxDraw>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>Resolves once the draw instance is ready</p></div> |
| `getAll`{lang="ts-type"} | `() => FeatureCollection \| undefined`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>Returns all current features as a collection</p></div> |
| `getMode`{lang="ts-type"} | `() => string \| undefined`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>Returns the current draw mode</p></div> |
| `add`{lang="ts-type"} | `(geojson: Feature \| FeatureCollection \| Geometry) => Promise<string[]>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>Adds features, syncs the model, and returns their ID list</p></div> |
| `deleteAll`{lang="ts-type"} | `() => Promise<void>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>Clears all features and syncs the model</p></div> |
| `changeMode`{lang="ts-type"} | `(mode: string) => Promise<void>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>Switches the draw mode and syncs the model</p></div> |
| `setFeatureProperty`{lang="ts-type"} | `(featureId: string, property: string, value: unknown) => Promise<void>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>Sets a feature's user_* property (drives theme styles) and syncs the model</p></div> |

## Changelog

:commit-changelog{prefix="components/extensions" name="DrawControl"}
