---
title: drawThemeStyles
description: Generate a mapbox-gl-draw styles array from theme options, with per-feature user_color overrides.
category: draw
seo:
  title: drawThemeStyles
  description: Generate a mapbox-gl-draw styles array from theme options, with per-feature user_color overrides.
links:
  - label: mapbox-gl-draw
    icon: i-simple-icons-mapbox
    to: https://github.com/mapbox/mapbox-gl-draw
    target: _blank
---

## Introduction

`drawThemeStyles` generates a complete mapbox-gl-draw `styles` array from theme options, intended for use with `userProperties: true`. Colors are resolved via `coalesce(user_color, themeColor)`, so a per-feature `user_color` property can override the theme color.

Auto-imported from `@movk/mapbox` — no manual import needed.

## Usage

Switch the primary color to generate theme styles (preset polygons update instantly):

::component-example
---
name: DrawThemeExample
prettier: true
collapse: true
clientOnly: true
options:
  - name: color
    label: color
    default: '#8b5cf6'
    items:
      - '#8b5cf6'
      - '#3b82f6'
      - '#10b981'
      - '#f43f5e'
---
::

## API

### `drawThemeStyles()`{lang="ts-type"}

Accepts `options: DrawThemeOptions` (all optional, defaults apply) and returns `StyleSpec[]`, which can be passed directly to [MapboxDrawControl](/docs/extensions/draw) via `options.styles`.

::field-group
  ::field{name="color" type="string"}
  Primary color for inactive features.
  @defaultValue `'#3b82f6'`
  ::
  ::field{name="activeColor" type="string"}
  Primary color for active (drawing / selected) features.
  @defaultValue `'#f59e0b'`
  ::
  ::field{name="fillOpacity" type="number"}
  Fill opacity for polygons.
  @defaultValue `0.1`
  ::
  ::field{name="lineWidth" type="number"}
  Line width.
  @defaultValue `2`
  ::
  ::field{name="vertexRadius" type="number"}
  Vertex circle radius.
  @defaultValue `5`
  ::
::

## Changelog

:commit-changelog{prefix="utils" name="draw-theme" suffix="ts"}
