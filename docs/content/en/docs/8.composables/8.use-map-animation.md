---
title: useMapAnimation
description: Map frame-animation primitive built on requestAnimationFrame; invokes the frame callback only when the map exists and the style is loaded.
category: interaction
seo:
  title: useMapAnimation
  description: Map frame-animation primitive built on requestAnimationFrame; invokes the frame callback only when the map exists and the style is loaded.
---

## Introduction

`useMapAnimation` is a map frame-animation primitive built on `useRafFn` (automatically stopped when the component unmounts). It invokes the `frame` callback only when the map exists and the style is loaded. The second argument to the callback is the elapsed milliseconds since the animation started. Animated effect components and frame-driven images all share this primitive. Returns `pause`, `resume`, and `isActive` to control the animation state.

::note
When used outside a `<MapboxMap>` subtree, specify the target map via `options.mapId`. The callback runs every frame — avoid creating new objects or doing heavy computation inside it.
::

## Usage

Drive a periodic `circle-radius` pulse on every frame:

::component-example
---
name: UseMapAnimationExample
prettier: true
collapse: true
clientOnly: true
---
::

## API

### `useMapAnimation()`{lang="ts-type"}

Creates a frame animation loop.

::field-group
  ::field{name="frame" type="(map: Map, elapsedMs: number) => void" required}
  Per-frame callback; `elapsedMs` is the elapsed milliseconds since the animation started.
  ::
  ::field{name="options.mapId" type="string"}
  Target map id; required when used outside a `<MapboxMap>` subtree.
  ::
  ::field{name="options.immediate" type="boolean"}
  Start immediately after creation, defaults to `true`.
  ::
::

Returns `UseMapAnimationReturn`:

::field-group
  ::field{name="pause" type="() => void"}
  Pauses the animation.
  ::
  ::field{name="resume" type="() => void"}
  Resumes the animation.
  ::
  ::field{name="isActive" type="Readonly<Ref<boolean>>"}
  Whether the animation is currently running.
  ::
::

## Changelog

:commit-changelog{prefix="composables" suffix="ts"}
