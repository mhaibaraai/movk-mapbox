---
title: useMaplibre
description: Look up a mounted map context by id from the registry to access the map outside its component tree or across routes.
category: context
seo:
  title: useMaplibre
  description: Look up a mounted map context by id from the registry to access the map outside its component tree or across routes.
---

## Introduction

`useMaplibre` is the escape hatch for [useMap](/docs/composables/use-map): it looks up a mounted map context by id from the registry, allowing access to the map outside the `<MaplibreMap>` component tree or even across routes. Returns `undefined` when no matching id is found.

::note
Only works with `<MaplibreMap>` instances that have an explicit `map-id` set (maps without an id are not registered). Registration happens at mount time, so this composable must be called after the map has mounted — a common pattern is to use it inside an event callback or within `whenLoaded()`.
::

## Usage

A toolbar located outside the `<MaplibreMap>` subtree accesses the map via `useMaplibre(id)` and flies to a target city:

::component-example
---
name: UseMaplibreExample
prettier: true
collapse: true
---
::

## API

### `useMaplibre()`{lang="ts-type"}

Looks up a map context by id from the registry.

::field-group
  ::field{name="id" type="string" required}
  The `map-id` of the target map.
  ::
::

Returns `MaplibreContext | undefined`: the context for the matching id (see [useMap](/docs/composables/use-map#api) for the shape), or `undefined` if not found.

## Changelog

:commit-changelog{prefix="composables" suffix="ts"}
