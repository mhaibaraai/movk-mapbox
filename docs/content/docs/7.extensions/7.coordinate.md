---
title: 坐标转换
description: 基于 gcoord 在 WGS84 / GCJ02 / BD09 / EPSG3857 间转换坐标点与 GeoJSON。
category: basemap
seo:
  title: Coordinate Transform
  description: Convert points and GeoJSON between WGS84, GCJ02, BD09 and EPSG3857 with transformPoint and transformGeoJSON, powered by gcoord.
links:
  - label: gcoord
    icon: i-simple-icons-github
    to: https://github.com/hujiulong/gcoord
    target: _blank
---

## 简介

`transformPoint` / `transformGeoJSON` 基于 [gcoord](https://github.com/hujiulong/gcoord) 在 WGS84 / GCJ02 / BD09 / EPSG3857 间转换坐标：前者转单个经纬度点，后者转任意 GeoJSON 并返回新对象（不修改入参）。常用于把来自高德 / 腾讯（GCJ02）、百度（BD09）的数据纠偏到 WGS84，对齐天地图与 Mapbox 底图。

::note
不同底图的坐标系不同，叠加数据前需统一：

- **天地图**：CGCS2000，与 WGS84 厘米级相容。`MapboxTiandituLayer` 走 `w`（EPSG:3857 球面墨卡托）瓦片，WGS84 数据**直接对齐，无需转换**。
- **高德 / 腾讯**：GCJ02（火星坐标，加密偏移），其数据叠到天地图 / Mapbox 前需 `GCJ02 → WGS84`。
- **百度**：BD09，需 `BD09 → WGS84`。
::

两者均从 `@movk/mapbox/utils/coordinate` 导入（非自动导入）。

## 用法

WGS84 坐标在天地图底图上直接对齐；把它转成 GCJ02（高德 / 腾讯坐标系）后叠在天地图上，反而会偏移：

::component-example
---
name: CoordinateExample
prettier: true
---
::

## 示例

### 纠偏 GeoJSON

把高德导出的 GCJ02 折线经 `transformGeoJSON` 转回 WGS84，对齐天地图底图。红线为原始 GCJ02（偏移），绿线为转换后 WGS84（对齐）：

::component-example
---
name: CoordinateGeoJsonExample
prettier: true
collapse: true
---
::

### 控制坐标精度

`precision` 指定输出坐标保留的小数位数，用于去抖与减小传输体积；省略则保留完整精度：

```ts
import { transformGeoJSON } from '@movk/mapbox/utils/coordinate'

const compact = transformGeoJSON(raw, 'GCJ02', 'WGS84', { precision: 6 })
```

## API

### `transformPoint()`{lang="ts-type"}

转换单个经纬度点，返回新坐标。

::field-group
  ::field{name="point" type="[number, number]" required}
  经纬度点 `[lng, lat]`。
  ::
  ::field{name="from" type="CRS" required}
  源坐标系。
  ::
  ::field{name="to" type="CRS" required}
  目标坐标系。
  ::
  ::field{name="options.precision" type="number"}
  输出坐标保留的小数位数，省略则保留完整精度。
  ::
::

返回 `[number, number]`：转换后的 `[lng, lat]`。

### `transformGeoJSON()`{lang="ts-type"}

转换任意 GeoJSON（点 / 线 / 面 / 集合），返回与入参同类型的新对象，不修改入参。

::field-group
  ::field{name="geojson" type="T" required}
  任意 GeoJSON 对象（`Feature` / `Geometry` / `FeatureCollection` 等）。
  ::
  ::field{name="from" type="CRS" required}
  源坐标系。
  ::
  ::field{name="to" type="CRS" required}
  目标坐标系。
  ::
  ::field{name="options.precision" type="number"}
  输出坐标保留的小数位数，省略则保留完整精度。
  ::
::

返回 `T`：与入参同类型的新 GeoJSON 对象。

### `CRS`{lang="ts-type"}

坐标系标识：`'WGS84'` | `'GCJ02'` | `'BD09'` | `'EPSG3857'`。

## Changelog

:commit-changelog{prefix="utils" suffix="ts"}
