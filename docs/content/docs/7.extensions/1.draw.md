---
title: MaplibreDrawControl
description: 基于 terra-draw 的声明式绘制控件，v-model 受控要素与模式，内置模式工具栏并暴露命令式方法。
category: draw
seo:
  title: MaplibreDrawControl Component
  description: Declarative drawing control built on terra-draw, with v-model features and mode, a built-in mode toolbar and imperative methods.
links:
  - label: terra-draw
    icon: i-simple-icons-github
    to: https://github.com/JamesLMilner/terra-draw
    target: _blank
---

## 简介

`MaplibreDrawControl` 基于 [terra-draw](https://github.com/JamesLMilner/terra-draw) 提供声明式绘制：`v-model:features` 受控要素集合（赋值即导入、绘制即回写）、`v-model:mode` 当前模式；触发 finish / delete / select / deselect / modechange 事件；并经 `defineExpose` 暴露命令式方法。缺省模式集合为 [movkDrawModes](/docs/extensions/draw-modes)，`controls` 控制内置工具栏，切换底图（`setStyle`）后自动恢复要素与模式。子组件可用 [useMaplibreDraw](/docs/composables/use-maplibre-draw) 注入绘制上下文；父地图设置 `map-id` 后，该 composable 亦可在组件树外按 id 驱动绘制。

::note
需额外安装可选依赖 `terra-draw` 与 `terra-draw-maplibre-gl-adapter`。模式名沿用 terra-draw 命名：`select` / `point` / `linestring` / `polygon` / `rectangle` / `circle` / `ellipse` / `sector`。
::

## 用法

用内置工具栏绘制点、线、面与规则图形，`v-model:features` 实时回写要素数：

::component-example
---
name: DrawControlExample
prettier: true
clientOnly: true
---
::

## 示例

### 命令式操作

经 [`useTemplateRef`](https://vuejs.org/api/composition-api-helpers.html#usetemplateref) 取实例，调用 `changeMode` / `deleteAll`（`controls` 设为 `false` 关闭内置工具栏）：

::component-example
---
name: DrawControlActionsExample
prettier: true
collapse: true
clientOnly: true
---
::

## API

### Props

:component-props{slug="MaplibreDrawControl"}

### Emits

:component-emits{slug="MaplibreDrawControl"}

### Slots

:component-slots{slug="MaplibreDrawControl"}

### Expose

通过 [`useTemplateRef`](https://vuejs.org/api/composition-api-helpers.html#usetemplateref) 访问组件实例。

| Name | Type |
| ---- | ---- |
| `draw`{lang="ts-type"} | `Readonly<Ref<TerraDraw \| undefined>>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>底层 TerraDraw 实例引用；挂载前为 undefined</p></div> |
| `whenReady`{lang="ts-type"} | `() => Promise<TerraDraw>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>绘制实例就绪时 resolve</p></div> |
| `getAll`{lang="ts-type"} | `() => FeatureCollection \| undefined`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>已完成的要素集合（不含绘制中的要素与辅助点）</p></div> |
| `getMode`{lang="ts-type"} | `() => string \| undefined`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>当前绘制模式</p></div> |
| `add`{lang="ts-type"} | `(geojson: Feature \| FeatureCollection \| Geometry) => Promise<FeatureId[]>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>添加要素并同步模型；缺失 id 时生成，缺失 properties.mode 时按几何类型推断，返回成功添加的 id</p></div> |
| `deleteAll`{lang="ts-type"} | `() => Promise<void>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>清空全部要素并同步模型</p></div> |
| `changeMode`{lang="ts-type"} | `(mode: string) => Promise<void>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>切换绘制模式并同步模型</p></div> |
| `setFeatureProperty`{lang="ts-type"} | `(featureId: FeatureId, property: string, value: unknown) => Promise<void>`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>设置要素属性（如 color 覆盖主题色）并同步模型</p></div> |

## Changelog

:commit-changelog{prefix="components/extensions" name="DrawControl"}
