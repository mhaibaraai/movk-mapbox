---
title: MapboxDrawControl
description: 基于 mapbox-gl-draw 的声明式绘制控件，v-model 受控要素与模式，并暴露命令式实例方法。
category: draw
seo:
  title: MapboxDrawControl Component
  description: Declarative drawing control built on mapbox-gl-draw, with v-model features and mode plus imperative instance methods.
links:
  - label: mapbox-gl-draw
    icon: i-simple-icons-mapbox
    to: https://github.com/mapbox/mapbox-gl-draw
    target: _blank
---

## 简介

`MapboxDrawControl` 基于 [mapbox-gl-draw](https://github.com/mapbox/mapbox-gl-draw) 提供声明式绘制：`v-model:features` 受控要素集合（赋值即导入、绘制即回写）、`v-model:mode` 当前模式；触发 create / update / delete / selectionchange / modechange 事件；并经 `defineExpose` 暴露命令式方法。子组件可用 [useMapboxDraw](/docs/composables/use-mapbox-draw) 注入实例。

## 用法

用内置工具栏绘制点 / 线 / 面，`v-model:features` 实时回写要素数：

::component-example
---
name: DrawControlExample
prettier: true
clientOnly: true
---
::

## 示例

### 命令式操作

经 [`useTemplateRef`](https://vuejs.org/api/composition-api-helpers.html#usetemplateref) 取实例，调用 `changeMode` / `deleteAll`（已关闭默认工具栏）：

::component-example
---
name: DrawControlActionsExample
prettier: true
collapse: true
clientOnly: true
---
::

## API

### Props

:component-props{slug="MapboxDrawControl"}

### Emits

:component-emits{slug="MapboxDrawControl"}

### Slots

:component-slots{slug="MapboxDrawControl"}

### Expose

通过 [`useTemplateRef`](https://vuejs.org/api/composition-api-helpers.html#usetemplateref) 访问组件实例。

| Name | Type |
| ---- | ---- |
| `draw`{lang="ts-type"} | `() => MapboxDraw \| undefined`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>获取底层 MapboxDraw 实例</p></div> |
| `getAll`{lang="ts-type"} | `() => FeatureCollection \| undefined`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>当前全部要素集合</p></div> |
| `add`{lang="ts-type"} | `(geojson: Feature \| FeatureCollection \| Geometry) => string[] \| undefined`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>添加要素，返回要素 id 列表</p></div> |
| `deleteAll`{lang="ts-type"} | `() => void`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>清空全部要素并同步模型</p></div> |
| `changeMode`{lang="ts-type"} | `(next: string) => void`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>切换绘制模式并同步模型</p></div> |
| `setFeatureProperty`{lang="ts-type"} | `(featureId: string, property: string, value: unknown) => void`{lang="ts-type"} <br> <div class="text-toned mt-1"><p>设置要素 user_* 属性（驱动主题样式）并同步模型</p></div> |

## Changelog

:commit-changelog{prefix="components/extensions" name="DrawControl"}
