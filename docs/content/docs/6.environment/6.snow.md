---
title: MapboxSnow
description: 为地图添加降雪效果，options 透传 mapbox snow 规格。
category: weather
seo:
  title: MapboxSnow Component
  description: Add a snow effect via setSnow, forwarding a Mapbox snow specification.
---

## 简介

`MapboxSnow` 调用 `setSnow` 为地图添加降雪效果，`options` 透传 mapbox snow 规格（`density`/`intensity`/`color` 等），省略用默认雪效。

::note{color="warning"}
`setSnow` 为 mapbox 实验性 API，需 v3 样式（如 `standard`），未来版本可能变更。
::

## 用法

倾斜视角下叠加降雪：

::component-example
---
name: SnowExample
prettier: true
---
::

## API

### Props

:component-props{slug="MapboxSnow"}

## Changelog

:commit-changelog{prefix="components/environment" name="Snow"}
