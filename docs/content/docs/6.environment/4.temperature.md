---
title: MapboxTemperature
description: 把温度采样点按属性渲染为热力层，支持权重范围、半径与配色断点。
category: weather
seo:
  title: MapboxTemperature Component
  description: Render temperature sample points as a heatmap layer with weight range, radius and color stops.
---

## 简介

`MapboxTemperature` 把 GeoJSON 点按温度属性渲染为 mapbox 原生 heatmap：`weightProperty` 取权重字段、`weightRange` 线性映射到热力权重，`radius`/`intensity`/`opacity` 与 `colorStops` 控制呈现。

## 用法

各城市温度采样点的热力分布，调整 `radius` 观察扩散：

::component-example
---
name: TemperatureExample
prettier: true
options:
  - name: radius
    label: radius
    default: 40
    items:
      - 20
      - 40
      - 60
---
::

## API

### Props

:component-props{slug="MapboxTemperature"}

## Changelog

:commit-changelog{prefix="components/environment" name="Temperature"}
