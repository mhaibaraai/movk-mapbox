---
title: MapboxGeolocateControl
description: 定位控件，点击后请求浏览器定位并在地图上跟踪用户位置与方向。
category: interactive
seo:
  title: MapboxGeolocateControl Component
  description: MapboxGeolocateControl requests browser geolocation and tracks the user position and heading on the map, forwarding options to the underlying GeolocateControl.
---

## 简介

`MapboxGeolocateControl` 提供定位按钮：点击后请求浏览器地理定位并将视图移至用户位置。`options` 直通 mapbox-gl 的 `GeolocateControl` 构造选项，`trackUserLocation` 持续跟踪、`showUserHeading` 显示方向锥、`positionOptions.enableHighAccuracy` 提升精度。

::note{color="warning"}
浏览器定位需在 HTTPS（或 localhost）下运行，并由用户在弹窗中授权；未授权或不支持时按钮不会移动视图。
::

## 用法

点击控件触发定位授权，授权后持续跟踪并显示方向：

::component-example
---
name: GeolocateControlExample
prettier: true
---
::

## API

### Props

:component-props{slug="MapboxGeolocateControl"}

## Changelog

:commit-changelog{prefix="components/controls" name="GeolocateControl"}
