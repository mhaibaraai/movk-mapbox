---
title: MaplibreBasemapControl
description: 底图切换控件，与地图的 options.style 绑定同一个值，天地图与矢量样式统一切换。
category: layer-management
seo:
  title: MaplibreBasemapControl Component
  description: MaplibreBasemapControl is a controlled basemap switcher bound to the same value as the map style, switching vector styles and Tianditu basemaps the same way.
---

## 简介

`MaplibreBasemapControl` 是受控组件：它只负责选择，通过 `v-model` 发出选中项的 `style`，不直接调用 `setStyle`。把同一个值绑定到 `MaplibreMap` 的 `options.style`，由地图完成切换，并自动重建叠加的业务图层。样式只存在一处，不会出现控件与地图状态不一致。

天地图底图用 [`tiandituStyle()`](/docs/extensions/tianditu) 生成完整样式，与 OpenFreeMap 等矢量样式放在同一个 `items` 列表里切换。

::tip
切换底图后，[`MaplibreLayerGroup`](/docs/core/layer-group) 的显隐与透明度（包括通过 `styleLayers` 认领的底图图层）会自动在新样式上恢复。
::

## 用法

::component-example
---
name: BasemapControlExample
prettier: true
---
::

## API

### Props

:component-props{slug="MaplibreBasemapControl"}

### Emits

:component-emits{slug="MaplibreBasemapControl"}

## Changelog

:commit-changelog{prefix="components/controls" name="BasemapControl"}
