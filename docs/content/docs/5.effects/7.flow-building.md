---
title: MaplibreFlowBuilding
description: 流动建筑，渐变着色加高亮带沿建筑高度循环流动。
category: building
seo:
  title: MaplibreFlowBuilding Component
  description: Flowing 3D buildings with gradient shading and a highlight band cycling along building height.
---

## 简介

`MaplibreFlowBuilding` 基于 `source` / `sourceLayer` 指定的建筑数据渲染 3D 建筑：渐变着色 + 高亮带沿建筑高度循环流动；`color` / `flowColor` / `duration` / `bandHeight` / `maxHeight` 控制外观与节奏。需倾斜视角且缩放 ≥ `minzoom`（默认 15）。

## 用法

倾斜视角下查看高亮带在楼体上流动：

::component-example
---
name: FlowBuildingExample
prettier: true
clientOnly: true
---
::

## API

### Props

:component-props{slug="MaplibreFlowBuilding"}

## Changelog

:commit-changelog{prefix="components/effects" name="FlowBuilding"}
