---
title: MaplibreWindowBuilding
description: 窗户建筑，程序生成窗户点阵贴图作 fill-extrusion-pattern。
category: building
seo:
  title: MaplibreWindowBuilding Component
  description: Window-lit 3D buildings using a procedurally generated window grid as fill-extrusion-pattern.
---

## 简介

`MaplibreWindowBuilding` 基于 `source` / `sourceLayer` 指定的建筑数据渲染 3D 建筑，并用程序生成的窗户点阵贴图作 `fill-extrusion-pattern`；`rows` / `cols` / `litColor` / `darkColor` / `litRatio` / `seed` 控制窗格外观。需倾斜视角且缩放 ≥ `minzoom`（默认 15）。

## 用法

夜色样式下点亮随机窗格：

::component-example
---
name: WindowBuildingExample
prettier: true
clientOnly: true
---
::

## API

### Props

:component-props{slug="MaplibreWindowBuilding"}

## Changelog

:commit-changelog{prefix="components/effects" name="WindowBuilding"}
