---
title: MaplibreRasterLayer
description: 栅格瓦片图层，叠加 XYZ/TMS 模板的外部瓦片服务。
category: data-driven
seo:
  title: MaplibreRasterLayer Component
  description: MaplibreRasterLayer adds an external XYZ/TMS raster tile service to the map with configurable tile size, scheme, zoom range and opacity.
---

## 简介

`MaplibreRasterLayer` 叠加外部栅格瓦片服务（`{z}/{x}/{y}` 模板 URL），支持 `tileSize` / `scheme` / `minzoom` / `maxzoom` / `attribution` / `opacity`。天地图等国内底图见 [扩展能力](/docs/extensions/tianditu)。

## 用法

在空样式底图上叠加 OpenStreetMap 瓦片：

::component-example
---
name: RasterLayerExample
prettier: true
---
::

## API

### Props

:component-props{slug="MaplibreRasterLayer"}

## Changelog

:commit-changelog{prefix="components/layers" name="RasterLayer"}
