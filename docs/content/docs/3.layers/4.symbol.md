---
title: symbol
description: 用 MaplibreLayer 的 symbol 类型渲染文字与图标标注，layout 读取要素属性。
category: builtin
seo:
  title: Symbol Layer
  description: Render text and icon labels with MaplibreLayer type="symbol", reading feature properties via text-field and layout expressions.
---

## 简介

`MaplibreLayer` 设 `type="symbol"` 渲染文字/图标标注：`text-field` 经 `['get', ...]` 读取属性，`text-size` / `text-offset` 等 layout 控制排布（图标需先经 [useMaplibreImage](/docs/composables/use-maplibre-image) 注册）。

::note{to="/docs/core/layer"}
symbol 由通用组件 MaplibreLayer 承载，完整 Props 与事件见其文档。
::

## 用法

按属性渲染文字标注，带描边便于深浅底图阅读：

::component-example
---
name: LayerSymbolExample
prettier: true
---
::

## API

### Props

:component-props{slug="MaplibreLayer"}

## Changelog

:commit-changelog{prefix="components" name="Layer"}
