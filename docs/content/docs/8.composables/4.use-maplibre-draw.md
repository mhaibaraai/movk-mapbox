---
title: useMaplibreDraw
description: 获取绘制上下文，在子组件或组件树外程序化控制绘制。
category: interaction
seo:
  title: useMaplibreDraw
  description: Access the MaplibreDrawControl context to switch modes and manage features, from inside the subtree or from anywhere via mapId.
---

## 简介

`useMaplibreDraw` 返回 `<MaplibreDrawControl>` 的绘制上下文：写操作（`changeMode` / `add` / `deleteAll` / `setFeatureProperty`）返回 `Promise`，内部等待实例就绪后执行，并同步控件的 `v-model:features` 与 `v-model:mode`；读操作（`getAll` / `getMode`）同步返回，未就绪时为 `undefined`。`draw` 字段保留 [TerraDraw](https://github.com/JamesLMilner/terra-draw) 裸实例作为逃生舱口。

::note
在 `<MaplibreDrawControl>` 子树外使用时，经 `options.mapId` 指定目标地图 —— 该地图需显式设置 `map-id`，否则不会登记进注册表。
::

## 用法

子组件经 `useMaplibreDraw()` 注入上下文并切换绘制模式；要素数量由控件的 `v-model:features` 回写：

::component-example
---
name: UseMaplibreDrawExample
prettier: true
collapse: true
clientOnly: true
---
::

## 示例

### 组件树外驱动

工具栏位于 `<MaplibreMap>` 之外，经 `options.mapId` 按 id 查注册表驱动绘制。这使全局面板、布局级组件无需参与地图组件树即可下发绘制指令：

::component-example
---
name: UseMaplibreDrawRemoteExample
prettier: true
collapse: true
clientOnly: true
---
::

## API

### `useMaplibreDraw()`{lang="ts-type"}

获取绘制上下文。

::field-group
  ::field{name="options.mapId" type="string"}
  目标地图 id；在 `<MaplibreDrawControl>` 子树外使用时必填。省略时注入最近的控件，取不到则抛错。
  ::
::

返回 `MaplibreDrawContext`：

::field-group
  ::field{name="mapId" type="string"}
  所属地图 id。
  ::
  ::field{name="draw" type="Readonly<Ref<TerraDraw | undefined>>"}
  绘制实例引用；控件挂载且地图 `load` 后才有值。
  ::
  ::field{name="whenReady" type="() => Promise<TerraDraw>"}
  实例就绪时 resolve。跨组件树调用且该 `mapId` 未注册控件时 reject。
  ::
  ::field{name="changeMode" type="(mode: string) => Promise<void>"}
  切换绘制模式并回写 `v-model:mode`。
  ::
  ::field{name="add" type="(geojson: Feature | FeatureCollection | Geometry) => Promise<FeatureId[]>"}
  添加要素并回写 `v-model:features`；缺失 id 时生成，缺失 `properties.mode` 时按几何类型推断，返回成功添加的 id。
  ::
  ::field{name="deleteAll" type="() => Promise<void>"}
  清空全部要素并回写 `v-model:features`。
  ::
  ::field{name="setFeatureProperty" type="(featureId: FeatureId, property: string, value: unknown) => Promise<void>"}
  设置要素属性（如 `color` 覆盖主题色）并回写 `v-model:features`。
  ::
  ::field{name="getAll" type="() => FeatureCollection | undefined"}
  已完成的要素集合（不含绘制中的要素与辅助点）；未就绪时为 `undefined`。
  ::
  ::field{name="getMode" type="() => string | undefined"}
  当前绘制模式；未就绪时为 `undefined`。
  ::
::

跨组件树调用时，若目标 `mapId` 尚未注册控件：写操作告警并空操作，读操作静默返回 `undefined`（它们常在 `computed` 中反复求值），仅 `whenReady()` 抛出明确错误。

## Changelog

:commit-changelog{prefix="composables" suffix="ts"}
