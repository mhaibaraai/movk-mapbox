---
title: bufferPaints
description: 生成缓冲区图层的默认 fill / line paint 对象，统一主色与不透明度。
category: geometry
seo:
  title: bufferPaints
  description: Build default fill and line paint objects for buffer layers from a shared color, fill opacity and line width.
---

## 简介

`bufferPaints` 按主色、填充不透明度与描边宽度生成一对 `fill` / `line` paint 对象，供缓冲区的填充层与描边层复用。`MapboxBufferCircle` 等[缓冲区组件](/docs/layers/buffer-circle)的默认样式即来自它，也可单独用于自绘的缓冲区图层。

从 `@movk/mapbox/utils/buffer` 导入（非自动导入）。

## 用法

把 `bufferPaints` 的输出分别绑定到 fill / line 图层：

::component-example
---
name: BufferPaintsExample
prettier: true
collapse: true
---
::

## API

### `bufferPaints()`{lang="ts-type"}

生成缓冲区默认 paint。

::field-group
  ::field{name="options.color" type="string"}
  主色（同时用于 fill 与 line），默认 `'#3b82f6'`。
  ::
  ::field{name="options.fillOpacity" type="number"}
  填充不透明度，默认 `0.25`。
  ::
  ::field{name="options.lineWidth" type="number"}
  描边宽度，默认 `2`。
  ::
::

返回 `{ fill, line }`：分别为填充层与描边层的 paint 对象，可直接绑定到 `<MapboxLayer>` 的 `paint`。

## Changelog

:commit-changelog{prefix="utils" suffix="ts"}
